spec_version: "1.0"
indicators:
    action_duration: metricSelector=builtin:apps.web.actionDuration.load.browser:filter(and(in("dt.entity.application",entitySelector("type(application),entityName.equals(~"PWA Dev~")")))):splitBy():avg
